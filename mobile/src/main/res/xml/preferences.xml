<?xml version="1.0" encoding="utf-8"?>
<!-- These are `android:accountPreferences` (still experimental). -->
<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">

    <Preference
        android:title="@string/title_preferences_token"
        android:summary="@string/summary_preferences_token"
        android:key="default"/>

    <SwitchPreference
        android:title="Debug Logs"
        android:summary="Verbose logging"
        android:key="logVerbose"/>

</PreferenceScreen>
