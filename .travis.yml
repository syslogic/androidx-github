language: android
jdk: oraclejdk8
sudo: false

env:
  global:
    - DISPLAY=:99.0
    - export ANDROID_SDK_ROOT=/usr/local/android-sdk
    - export ANDROID_HOME=/home/travis/.android

before_install:
  - export PATH=${ANDROID_SDK_ROOT}/tools/bin:$PATH

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.android/build-cache

android:
  components:
    - android-28
    - sys-img-x86-google_apis-28
    - build-tools-28.0.3

before_script:
  - echo no | android create avd --force -n test -t android-28 --abi google_apis/x86
  - emulator -avd test -no-audio -no-window -verbose &
  - android-wait-for-emulator
  - adb shell input keyevent 82 &

script: ./gradlew mobile:assembleDebug connectedAndroidTest
